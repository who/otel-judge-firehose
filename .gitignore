
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
*.gate.lock*

# BEGIN ortus block=gitignore schema=1 generated-by=ortus@0.4.1.dev31+g6b24df3
# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Environment files
.env
.env.*
!.env.example

# Credentials and secrets
credentials.json
*_secret*
*.pem
*.key

# OS
.DS_Store
Thumbs.db

# Beads viewer local config
.bv/

# bd runtime lock at the repo root (bd >= 1.2) — tool state, never source
.beads.gate.lock

# Human-decision report (regenerated by `ortus human`)
HUMAN-TODO.md

# Build artifacts and logs
*.log
logs/

# Cache directories
.cache/

# CodeGraph index — local, machine-specific, and large. Every fresh clone
# rebuilds it with `codegraph init`; `ortus check` reports it when missing.
.codegraph/

# Python / Node / Go / Rust common build outputs
__pycache__/
*.py[cod]
.venv/
node_modules/
dist/
build/
target/
# END ortus block=gitignore

# Wrangler local state (dev server, build cache)
.wrangler/

# Wrangler local secrets (copy from .dev.vars.example)
.dev.vars
!.dev.vars.example
